{
  "type": "table",
  "properties": {
    "name": "${tables.roles}",
    "columns": {
      "id": {
        "type": "bigint",
        "unique": true,
        "primary": true,
        "default": "${defaultPrimaryKey}"
      }
    }
  }
}
