{
  "type": "table",
  "properties": {
    "name": "${tables.roles}",
    "columns": [
      {
        "name": "id",
        "type": "bigint",
        "unique": true,
        "primaryKey": true,
        "default": "${defaultPrimaryKey}"
      }
    ]
  }
}
