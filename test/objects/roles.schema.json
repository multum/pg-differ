{
  "type": "table",
  "properties": {
    "name": "${tables.roles}",
    "columns": {
      "id": {
        "type": "bigint",
        "unique": true,
        "primaryKey": true,
        "default": "${defaultPrimaryKey}"
      }
    }
  }
}
