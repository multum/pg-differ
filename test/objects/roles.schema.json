{
  "type": "table",
  "properties": {
    "name": "${ROLES}",
    "columns": [
      {
        "name": "id",
        "type": "bigint",
        "unique": true,
        "primaryKey": true,
        "default": "${defaultPrimaryKey}"
      }
    ]
  }
}
