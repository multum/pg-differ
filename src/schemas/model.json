{
  "required": [
    "name",
    "columns"
  ],
  "definitions": {
    "extension": {
      "type": "object",
      "properties": {
        "columns": {
          "type": "array"
        }
      }
    },
    "foreignKey": {
      "type": "object",
      "properties": {
        "columns": {
          "type": "array"
        },
        "match": {
          "type": "string",
          "enum": [
            "SIMPLE",
            "FULL",
            "PARTIAL"
          ]
        },
        "onDelete": {
          "type": "string",
          "enum": [
            "RESTRICT",
            "CASCADE",
            "NO ACTION"
          ]
        },
        "onUpdate": {
          "type": "string",
          "enum": [
            "RESTRICT",
            "CASCADE",
            "NO ACTION"
          ]
        },
        "references": {
          "type": "object",
          "required": [
            "table",
            "columns"
          ],
          "properties": {
            "table": {
              "type": "string"
            },
            "columns": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "column": {
      "required": [
        "name",
        "type"
      ],
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "unique": {
          "type": "boolean"
        },
        "index": {
          "type": "boolean"
        },
        "primaryKey": {
          "type": "boolean"
        },
        "nullable": {
          "type": "boolean"
        },
        "force": {
          "type": "boolean"
        },
        "formerNames": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "autoIncrement": {
          "oneOf": [
            {
              "type": "boolean"
            },
            {
              "$ref": "sequence"
            }
          ]
        }
      }
    }
  },
  "properties": {
    "name": {
      "type": "string"
    },
    "forceExtensions": {
      "type": "array",
      "items": {
        "enum": [
          "foreignKey",
          "primaryKey",
          "unique",
          "index",
          "check"
        ]
      }
    },
    "indexes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/extension"
      }
    },
    "unique": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/extension"
      }
    },
    "primaryKeys": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/extension"
      }
    },
    "foreignKeys": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/foreignKey"
      }
    },
    "columns": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/column"
      }
    }
  }
}
